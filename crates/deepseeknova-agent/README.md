# deepseeknova-agent

Agent implementations — the brains of deepseeknova. Each agent type implements
`Runner` and can be plugged into the runtime. Includes the main agent loop,
coordinator, plan-mode runner, and sub-agent delegation with memory management.
