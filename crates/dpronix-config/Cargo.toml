[package]
name = "dpronix-config"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "Layered TOML configuration with merge semantics (defaults, user, project, env, CLI flags)."
readme = "README.md"
documentation = "https://docs.rs/dpronix-config/0.3.0"

[dependencies]
anyhow = { workspace = true }
serde = { workspace = true }
serde_json = { workspace = true }
toml = { workspace = true }
dirs = { workspace = true }

[dev-dependencies]
tempfile = { workspace = true }
serde_json = { workspace = true }
