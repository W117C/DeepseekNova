[package]
name = "dpronix-event"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "Agent lifecycle event types and pub-sub event bus for tool calls, model responses, and errors."
readme = "README.md"
documentation = "https://docs.rs/dpronix-event/0.3.0"

[dependencies]
tokio = { workspace = true }
serde = { workspace = true }
serde_json = { workspace = true }
dpronix-core = { version = "0.3.0", path = "../dpronix-core" }
