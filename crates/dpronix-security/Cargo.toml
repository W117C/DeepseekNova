[package]
name = "dpronix-security"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "Capability-based access control, path confinement, allow-lists, resource limits, and audit logging."
readme = "README.md"
documentation = "https://docs.rs/dpronix-security/0.3.0"

[dependencies]
anyhow = { workspace = true }
tracing = { workspace = true }
dpronix-core = { version = "0.3.0", path = "../dpronix-core" }

[dev-dependencies]
tempfile = { workspace = true }
