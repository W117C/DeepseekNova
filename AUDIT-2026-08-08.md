# DeepseekNova v0.5.0 发布后全面审计报告

- 日期：2026-08-08
- 方式：6 路并行只读检查代理（代码健康 / 安全 / 文档一致性 / 发布工程 / 架构 / 功能自洽）+ 综合代理；父级复核关键发现（make check / 文件证据）
- 审计基线：HEAD `d752ebc`（v0.5.0 发布态）+ 工作树另一会话的未提交改动（分别评估）

## 修复状态（本报告入库时更新）

**已修复（提交 c934c8f，2026-08-08）**：C1（撤销 [[bin]] 改名，保持
deepseeknova-cli）、H1（macos-13→macos-15-intel）、H2（README 最新 v0.5.0）、
H3（GUIDE 权限默认 true，两处）、M6（[ui] lang merge）、M10a/b/c/g/i/j
（文档批次）、M11a（删 cargo-dist 死配置）、M11b（workflow_dispatch 接线）。

**待修复（需决策或依赖另一会话稳定）**：H4（runtime embed 闭包 3 处）、M1
（审计盲区）、M2（死代码三件套）、M3（markdown 通道）、M4（三选一裁决）、
M5（engine 递归深度）、M7（大文件拆分 L）、M8（测试密度）、M9（extract_json）、
M10d/e/f（工具数/功能文档）、M11c（ci.yml release job）、L1-L13。

**协调项**：工作树 28 项改动属另一会话进行中重做（delegate_tool.rs 迁移、
子代理 ToolHook 链等），方向正确、make check 全绿，但含退出码/工具数/注释
变更需随其提交同步 CHANGELOG 与文档。

---

## 1. 执行摘要

v0.5.0 发布本身是成功的：5 平台资产 + checksums.txt 端到端自洽（实测 linux x86_64 归档 SHA256 与发布校验一致、归档内为 `deepseeknova-cli`），22 个 crate 版本统一，cargo deny 全绿，git 卫生良好。代码健康度良好：全仓库零 unsafe、仅 2 个生产 TODO、约 1607 条测试、五层深度防御（沙箱/权限门/安全策略/资源限额/审计）真实接通且默认姿态整体 fail-closed，readonly 分类器与路径守卫覆盖全部历史修复项并有回归测试，无凭据入库。

但存在两个方向的问题需要立即处理：
1. **文档面**：2 处 high 级误导（README 声称最新发布仍为 v0.4.0；GUIDE 权限 `enabled` 默认值注释与代码相反，属安全姿态误导）+ 一批 medium 漂移。
2. **发布工程面**：两个威胁下次发布的隐患（release.yml 的 macos-13 runner 已不在 GitHub 当前托管清单；工作树另一会话的进行中改动新增 `[[bin]] name="deepseeknova"`，一旦未同步发布契约就提交将直接破坏 install/release/CI 资产名契约）。

工作树改动整体方向正确且静态自洽（delegate 工具迁移消除依赖反转、子代理 ToolHook 链加固、error.rs 删除无悬挂引用），但不属于稳定态，合入前必须同步发布契约与文档。

最需立即做的是：提交工作树前统一二进制命名与退出码文档、迁移 release runner、修正两处安全姿态误导文档。

---

## 2. 问题清单（按严重度排序）

### 🔴 Critical

**C1. 工作树未提交的 `[[bin]] name = "deepseeknova"` 破坏发布/安装资产契约**
- 位置：`crates/deepseeknova-cli/Cargo.toml:11-13`（新增 `[[bin]]`）；对侧契约 `release.yml:74-78`（打包 `deepseeknova-cli`）、`install.sh:152/156`、`install.ps1:114/120`、`ci.yml:133`（glob `deepseeknova-cli*` 且 if-no-files-found: error）
- 交叉核实：3 路确认。HEAD 与 tag d1c0cf2 均无此块，v0.5.0 实测归档内为 `deepseeknova-cli`
- 影响：一旦合入——release.yml 打包步找不到 `deepseeknova-cli` 而失败、install 脚本期望的资产名不匹配、ci.yml 每次 main push 直接红；三处发布链路同时断
- 修复建议（M）：提交前决策二进制名。保持 `deepseeknova-cli` 则撤销该块；更名则必须同步 release.yml / install.sh / install.ps1 / ci.yml 的 glob / README 与 AGENTS 安装节，并重跑一次发布验证。不得在契约未同步时发布

### 🟠 High

**H1. release.yml 的 macos-13 runner 已不在 GitHub 当前托管 runner 清单**
- 位置：`.github/workflows/release.yml:52`（`os: macos-13`）及 :30-36 注释
- 实证：当前 Intel macOS 标签为 macos-15-intel / macos-26-intel；与 v0.5.0 发布体自述「Intel Mac 任务在 GitHub Intel runner 排队中、需人工补传」现象吻合
- 影响：下次 tag 发布时 Intel macOS 作业可能直接失败或长期排队，重演人工补传
- 修复（S）：`macos-13` → `macos-15-intel`，同步注释；发布前 workflow_dispatch 干跑验证

**H2. README/README_EN 声称「当前最新发布为 v0.4.0、仅 3 平台」，实际 v0.5.0 已发布（5 平台）**
- 位置：`README.md:200`、`README_EN.md:129`（已核实原文）
- 影响：用户读到会以为只能下载 3 平台旧版，属声称不实
- 修复（S）：改为「v0.5.0 已发布、覆盖全部 5 平台」

**H3. GUIDE 权限配置注释声称 `enabled` 默认 false，代码默认 true（安全姿态误导）**
- 位置：`GUIDE.md:186`（已核实原文）；对照 `config/src/lib.rs:890`（`#[serde(default = "default_true")]`）
- 影响：读者会误以为权限门控默认关闭，与默认安全姿态相悖
- 修复（S）：注释改为「true（默认，默认安全姿态）」

**H4. P2-5 遗留：runtime 3 处同步闭包在 async 主循环内 block_on 调用同步 embed，阻塞 tokio worker**
- 位置：`runtime/src/lib.rs:630`（起点召回）、`:730`（中途召回）、`:843`（DistillHook）→ `core/memory/store.rs:812` → `provider/embeddings.rs:131-137`（block_on）
- 触发条件：仅 `[memory] embedder="remote"` 时生效（条件性 high）
- 影响：remote embedder 配置下 run 起点/中途/结束各阻塞一次，整条 agent 运行停顿并占一个 tokio worker
- 修复（M）：三处闭包包 `spawn_blocking` 并改调 `embed_async`（与 tools 同模式），补线程 id 回归测试

### 🟡 Medium

- **M1** 审计层盲区：gate 层拒绝（越界硬拒/危险命令/deny 规则/限流）未持久化到 `.deepseeknova/security/audit.jsonl`（仅 enforce_capability 覆盖）。修复（M）：PermissionGate 注入 AuditLogger
- **M2** 死代码三件套：Memory repeat-guard（agent/memory.rs:168-190）、Coordinator goal_mode（coordinator.rs:412）、PhaseRunner current_phase（phase_runner.rs:395）。修复（S）：删除或接线
- **M3** `.deepseeknova/agents/*.md` markdown 子代理声明通道是死代码（文档声称双通道，实际仅 TOML 接线）——2 路确认。修复（M）：接线 load_dir 或文档降级
- **M4** Runtime/EventBus/ContextEngine「三选一」仍未裁决 + event crate 生产零消费者。修复（M）：删除或标注（P2-6 同款）
- **M5** delegate 引擎路径递归深度不传播，max_depth 守门在生产路径失效（build_delegate_engine 硬编码剔除 delegate、agent.rs:850 固定 DelegateDepth(1)）——2 路确认。修复（M）：声明禁递归 + 传 max_depth，或注入 RecursiveDelegateTool
- **M6** `[ui] lang` 配置层死通道：Config::merge 丢弃 self.ui，只有 DEEPSEEKNOVA_LANG 生效（config/lib.rs:1778-1815）。修复（S）：merge 补 self.ui.lang
- **M7** agent.rs 7188 行 / runtime lib.rs 4631 行单文件结构债——2 路确认。修复（L）：抽 run_loop.rs/turn.rs + runtime 拆私有构造子函数
- **M8** 高复杂度 crate 测试密度薄：graph 12.0 / runtime 12.7 / cli 13.2 tests-per-1kLOC。修复（M）：目标 ≥15
- **M9** agent crate 内 3 份逐字节重复 extract_json + coordinator/scanner 近似实现。修复（S）：统一
- **M10** 文档漂移批次：
  - **M10a** CHANGELOG v0.5.0 桌面脚手架残留（:40-43）+ 缺 190ac01 主题条目
  - **M10b** README「委派禁递归」过时（P1-5 已放开）
  - **M10c** README 测试数 1571 过时（实际约 1607）
  - **M10d** 工具数 17 vs 16（工作树 delegate 移入 agent 后）——2 路确认
  - **M10e** GUIDE 未覆盖已落地功能（hooks/权限预设/信任/audit/预算/network_allow_domains//rename /checkpoint）
  - **M10f** README 功能清单遗漏 worktree/hooks/i18n/记忆用户面/语义检索
  - **M10g** SECURITY.md 版本表仍 0.4.x Current
  - **M10h** BACKEND_AUDIT.md 存在当前时态桌面残留
  - **M10i** tui README Design 节仍写 Codex cyan/magenta（190ac01 已改 Claude Code 观感）
  - **M10j** PRODUCT.md:39「15 个斜杠命令」实为 18；GUIDE.md:1032「Ctrl+1..5」与实现不符
- **M11** 发布工程中等项：
  - **M11a** `[workspace.metadata.dist]` 陈旧且与 release.yml 不符（4 平台、npm 声明但无 job）
  - **M11b** release.yml workflow_dispatch 的 version 输入未接线（手动触发会建「Release main」）
  - **M11c** ci.yml release job 每次 main push 白跑 3 平台 release 构建

### ⚪ Low / Info

- **L1** CHANGELOG 退出码文本过期（工作树将 paused 3→10、config 错误 2→6）——2 路交叉，正向消除与 eval_exit_code 碰撞，合入前需写 CHANGELOG
- **L2** 库路径 `.build().expect()`：tools docs_tools.rs:36 / web_fetch.rs:152 / web_search.rs:42
- **L3** Mutex lock `.unwrap()`：sub_agent.rs:310/:508、tui app/mod.rs:469
- **L4** DelegateTool 无能力门禁（裸装配 DelegateEngine 时可绕过）
- **L5** readonly 多词白名单仍按 starts_with 接受尾部任意 flag（ssh-keygen -y 等），无回归测试
- **L6** AGENTS.md thiserror 指引仅 5/22 crate 执行
- **L7** 两套生产 git 封装（review.rs / cli worktree.rs）
- **L8** 主对话 @-mention 入口拦截未接线；with_user_hooks 在 SubAgentRunner 已实现但 runtime 未接线
- **L9** dotenvy 死依赖；scanner 无 readme 字段；20 crate 硬编码 documentation=/0.5.0
- **L10** release.sh 过时（错误二进制名、漏 3 crate）；bump-version.sh 不更新内部版本钉
- **L11** secure_resolve 词法判定对 root 侧未 normalize（fail-closed 方向，macOS /var↔/private/var 误拒）
- **L12** 沙箱默认关闭（已文档化权衡）；Windows 无 OS 沙箱
- **L13** `.impeccable/questions/012cd8e1.log` 被 git 跟踪；v0.5.0 tag 落后 HEAD 4 提交；GUIDE「Rust 1.75」与 README 徽章 1.97 不一致

---

## 3. 优点与正向资产

- **安全姿态真实可信（非文档声称）**：五层防御端到端接通——沙箱（后端缺失 fail-closed）、权限门（agent/coordinator/sub_agent 全接线，preflight 含路径守卫+只读分类+畸形 JSON fail-closed）、安全策略（shell/web_fetch 消费 + redirect 逐跳复验）、资源限额、审计。默认姿态 fail-closed：enabled=true、default_mode=Ask、untrusted 工作区 allow 降级、子代理 Ask 视为拒绝
- **安全面覆盖完整性**：readonly 分类器覆盖全部历史修复项（/dev/tcp、gh api 隐式 POST、git 注入、env/xargs/find 执行器、gpg --export-secret-keys 等）并有专项回归；sanitize_output 幂等、Unicode 安全；路径守卫统一单一事实源
- **凭据与工程卫生**：全仓库 git 跟踪文件无真实凭据；.gitignore 覆盖完整；22 crate 版本统一 0.5.0；cargo deny + cargo-audit 双通道全绿；install.sh/install.ps1 质量高（SHA256 校验、tmp 清理、平台映射正确）；v0.5.0 发布端到端自洽
- **代码健康**：零 unsafe、仅 2 个生产 TODO、1607 测试；安全敏感 crate 测试密度强（permission 23.7 / security 25.8 / sandbox 33.0）
- **架构**：跨 crate 依赖无循环，四层分层成立；graph 语义检索接线链完整且 fail-open 三级回落正确；SubAgentRunner 递归路径正确注入真实深度
- **功能闭合度高（11 维自洽）**：权限模式三档、工作区信任、eval 分级、/rename /checkpoint、记忆用户面、用户级 hooks、exec 审计、i18n 渲染面均实现并接线到消费点，多数有测试背书
- **工作树改动方向正确**：delegate 工具迁入 agent crate 消除 tools→agent 依赖反转、子代理 ToolHook 链 + catch_unwind 契约（before 失败→Deny fail-closed）、error.rs 删除无残留

---

## 4. 工作树未提交改动评估

来源为另一会话的进行中重做，6 个代理静态核对结论一致：**方向正确、内部自洽、不破坏当前稳定态（未合入；父级 make check 全绿），但存在必须协调的发布契约与文档不同步。**

| 改动 | 评估 |
|---|---|
| `agent/src/delegate_tool.rs` 新增（DelegateTool 从 tools 迁入 agent） | ✅ 正向，消除依赖反转 |
| `core/src/error.rs` 删除 | ✅ 正向，无残留引用 |
| `tools/src/delegate.rs` 删除 + 子代理 ToolHook 链 | ✅ 正向，fail-closed 设计正确 |
| `permission` 路径检查委托 `security::path::secure_resolve` | ✅ 正向，单一事实源 |
| `cli/Cargo.toml` 新增 `[[bin]] name="deepseeknova"` | ⚠️ **破坏发布契约**（C1，3 路确认） |
| 退出码 paused 3→10、config 错误 2→6 | ⚠️ 消除与 eval_exit_code 碰撞（正向），但与已发布 CHANGELOG 冲突（L1） |
| 工具数 17→16 | ⚠️ 代码已同步，文档未同步（M10d） |

---

## 5. 优先级行动清单

### 立即（发布后第一件事）
1. 统一工作树二进制命名契约（C1）
2. 修正两处 high 级文档误导：README「最新 v0.4.0」→ v0.5.0（H2）；GUIDE 权限默认 true（H3）
3. 迁移 release runner：macos-13 → macos-15-intel（H1）
4. 提交前同步 CHANGELOG：退出码（L1）、工具数（M10d）、桌面脚手架标注 + Claude Code 主题条目（M10a）
5. 对 C1 子代理重构补聚焦测试（sub_agent + no-stub 回归）
6. 修 `[ui] lang` 配置死通道（M6）

### 近期（下一迭代）
- 死代码清理批次（M2/M3）；审计层覆盖 gate 拒绝（M1）；DelegateTool 能力门禁（L4）
- P2-5 embed_async 迁移（H4）；delegate 引擎深度传播裁决（M5）
- 「三选一」裁决（M4）；workflow_dispatch/ci.yml release job 处理（M11b/M11c）；metadata.dist 清理（M11a）
- 文档漂移批量（M10e-M10j）；测试密度提升 graph/runtime（M8）

### 长期（技术债）
- 拆分 agent.rs / runtime lib.rs（M7，独立重构 PR）；统一 extract_json（M9）
- 退役 release.sh、修 bump-version.sh 内部版本钉（L10）；参数化 docs.rs 链接（L9）
- 双 git 封装收敛（L7）；thiserror 策略裁决（L6）；库路径 expect/mutex unwrap 硬化（L2/L3）

---

## 6. 整体评分

| 维度 | 分数 | 一句话理由 |
|---|---|---|
| 代码质量 | 8/10 | 结构清晰、测试扎实、零 unsafe、无循环依赖；agent.rs 7188 行/runtime 4631 行单文件与部分 crate 测试密度薄是负项 |
| 安全性 | 9/10 | 五层防御真实接通 + fail-closed 默认姿态 + 历史缺陷全修复有回归；扣在 gate 拒绝审计盲区与 DelegateTool 裸装配无门禁 |
| 文档 | 5/10 | 「最新 v0.4.0」与「权限默认 false」两处 high 级误导 + 一批 medium 漂移，是本次审计最弱环节 |
| 发布就绪 | 7/10 | v0.5.0 已成功且端到端自洽；工作树二进制改名 + macos-13 runner 退役双隐患威胁下次发布 |
| 架构 | 8/10 | 分层成立、无循环依赖、语义检索 fail-open 正确；「三选一」未裁决、P2-5 遗留与巨型装配函数是待清偿债 |

**总评**：产品处于健康、可安全继续演进的稳定态。当前最紧迫的威胁不是已发布代码，而是「工作树改动在契约未同步时合入」与「文档面的两处安全姿态误导」——两者都应在下一 commit 前处理完毕。
